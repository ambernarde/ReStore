import { Typography } from "@mui/material";

export default function CheckoutPage(){
    return(
        <Typography variant='h3'>
               Only logged in users should be albe to see this!
        </Typography>
    )
}